# splunk — description optimizer results

Generated by skill-creator's `scripts/run_loop.py` against `trigger-evals.json`.

- **exit_reason:** `max_iterations (5)`
- **iterations_run:** 5
- **train/test split:** 12/8 (holdout 0.4)
- **best_train_score:** 6/12
- **best_test_score:** 4/8

- **Optimizer chose original?** yes — none of the proposed iterations beat the seeded description on the held-out test set

## Iteration history

### iteration 1 — train 6/12, test 4/8

```
Query Mozilla Splunk Cloud for Azure activity logs (VM lifecycle, NIC/disk writes, deletes, OSProvisioningTimedOut) on Taskcluster worker pools — driving Splunk Web through `browser-harness` against an authenticated Chrome tab because the REST API is disabled at Mozilla. Use for VM-side provisioning failures and worker lifespan on `index=azure_audit`. DO NOT USE FOR in-VM logs (use papertrail) or worker-manager service decisions (use tc-logview).
```

### iteration 2 — train 6/12, test 4/8

```
Use this skill whenever the user wants to query Splunk, run an SPL search, or pull Azure activity log events for Mozilla Taskcluster worker VMs. Triggers on mentions of "Splunk", "SPL", "tstats", `index=azure_audit`, "azure activity log", "azure audit", or any request to look up VM lifecycle events (creates, deletes, writes, failures) for a worker pool, resource group (e.g. `RG-TC-GECKO-T-WIN11-64-25H2`, `RG-TASKCLUSTER-WORKER-MANAGER-PRODUCTION`), or specific VM ID (`vm-...`). Use it to investigate VM-side provisioning failures (`OSProvisioningTimedOut`, `Failed.OperationPreempted`, spot evictions, spot pricing), build VM timelines, confirm what Azure did to a worker, or filter by `operationName`/`resultType`/`resultSignature`. Do NOT use for in-VM Windows logs (use papertrail), worker-manager provisioning decisions (use tc-logview), Taskcluster task logs/artifacts (use taskcluster), or BigQuery cost data.
```

### iteration 3 — train 6/12, test 4/8

```
Use this skill to query Splunk — specifically Mozilla's Splunk Cloud Azure activity log index (`index=azure_audit`) — for VM lifecycle events on Taskcluster worker pools. Trigger on any mention of Splunk, SPL, `tstats`, `azure_audit`, "azure activity log", or "azure audit". Also trigger when the user wants to look up Azure-side VM creates/deletes/writes/failures, build a VM timeline, investigate spot evictions, spot pricing, `OSProvisioningTimedOut`, `Failed.OperationPreempted`, or other `resultType`/`resultSignature` outcomes for a worker pool, resource group (e.g. `RG-TC-GECKO-T-...`, `RG-TASKCLUSTER-WORKER-MANAGER-PRODUCTION`), or a specific `vm-...` ID. Also trigger when a user wants to confirm in Splunk what Azure did to a worker after seeing a signal in tc-logview, papertrail, or Treeherder. Do NOT use for in-VM Windows logs (papertrail), worker-manager provisioning decisions (tc-logview), Taskcluster task logs/artifacts (taskcluster skill), or BigQuery cost queries.
```

### iteration 4 — train 6/12, test 4/8

```
Use for any Splunk query against Mozilla's Splunk Cloud, especially the `index=azure_audit` Azure activity log covering Taskcluster worker VM lifecycle. Triggers: "Splunk", "SPL", "tstats", "azure_audit", "azure activity log", "azure audit", searches scoped to a worker resource group (`RG-TC-GECKO-T-*`, `RG-TASKCLUSTER-WORKER-MANAGER-PRODUCTION`) or a `vm-...` ID, and Azure-side outcomes like `OSProvisioningTimedOut`, `Failed.OperationPreempted`, spot evictions, spot pricing changes, VM creates/deletes/writes/failures, `operationName`/`resultType`/`resultSignature` filters, or building a VM timeline. Also triggers when the user wants Splunk to confirm what Azure did to a worker after a hint from tc-logview, papertrail, Treeherder, or a bug report. Do NOT use for in-VM Windows logs (papertrail), worker-manager provisioning decisions (tc-logview), Taskcluster task logs/artifacts (taskcluster skill), or BigQuery cost queries.
```

### iteration 5 — train 6/12, test 4/8

```
Use this skill whenever the user wants to run a Splunk search, query Mozilla's Splunk Cloud, or look at Azure activity logs for Taskcluster worker VMs. This is THE skill for anything touching `index=azure_audit`, SPL, `tstats`, or the Azure-side view of worker VM lifecycle (creates, deletes, writes, NIC/disk operations, failures).

Trigger on: any mention of Splunk or SPL; queries scoped to a worker resource group like `RG-TC-GECKO-T-*` or `RG-TASKCLUSTER-WORKER-MANAGER-PRODUCTION`; any `vm-...` ID timeline in Azure; Azure outcomes such as `OSProvisioningTimedOut`, `Failed.OperationPreempted`, spot evictions, spot pricing changes, or filters on `operationName`/`resultType`/`resultSignature`; corroborating an Azure-side action after a hint from tc-logview, papertrail, Treeherder, or a bug.

Skip for: in-VM Windows logs (papertrail), worker-manager decisions (tc-logview), task logs/artifacts (taskcluster skill), or BigQuery cost queries.
```
