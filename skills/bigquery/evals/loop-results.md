# bigquery — description optimizer results

Generated by skill-creator's `scripts/run_loop.py` against `trigger-evals.json`.

- **exit_reason:** `max_iterations (5)`
- **iterations_run:** 5
- **train/test split:** 12/8 (holdout 0.4)
- **best_train_score:** 6/12
- **best_test_score:** 4/8

- **Optimizer chose original?** yes — none of the proposed iterations beat the seeded description on the held-out test set

## Iteration history

### iteration 1 — train 6/12, test 4/8

```
Run ad-hoc SQL against Mozilla telemetry tables using the bq CLI. Use when the task needs raw BigQuery access — dry-run cost checks, custom SQL, pulling DAU/MAU/Windows-distribution/Glean-metric data into a script. DO NOT USE FOR saved/shared queries with visualizations (use redash) or finding which probe to query (use mozdata:probe-discovery).
```

### iteration 2 — train 6/12, test 4/8

```
Execute SQL against Mozilla telemetry from the command line via the `bq` CLI — actually running the query, not just writing one. Triggers when the user wants results in the terminal: dry-runs to estimate scan cost, ad-hoc counts of clients/DAU/MAU, OS or Windows/macOS/Linux version distributions, Glean metric pulls, joins across telemetry tables, sample_id checks, partition-filter cost optimization, or piping query output into a script. Mentions of "bq", "BigQuery CLI", "run this query", "dry-run", `moz-fx-data-shared-prod`, `baseline_clients_daily`, `events_stream`, or `active_users_aggregates_v3` are strong signals. Skip for: saved/shared queries with charts (use redash), finding which probe to instrument or query (use mozdata:probe-discovery), or pure query-authoring guidance without execution (use mozdata:query-writing).
```

### iteration 3 — train 6/12, test 4/8

```
Use to execute BigQuery SQL against Mozilla telemetry from the terminal via the `bq` CLI. This is the right skill whenever the user wants query results in the shell, not a web chart — running SELECTs, dry-runs to estimate scan bytes, counting clients/DAU/MAU, pulling OS or Windows/macOS/Linux version breakdowns, Glean metric extracts, joining telemetry tables, sample_id sanity checks, or piping output into a script. Strong signals: "bq", "use bq", "run this query", "dry-run", "mozdata", "shared-prod", `baseline_clients_daily`, `active_users_aggregates_v3`, `events_stream`, or any pasted SQL the user wants executed. Also fires when the user asks which Mozilla telemetry table is cheapest/right for a count and expects you to actually run it. Skip when: results need to be saved/visualized/shared (use redash), the user only wants to find a probe (use mozdata:probe-discovery), or the user wants query authoring help with no execution intent (use mozdata:query-writing).
```

### iteration 4 — train 6/12, test 4/8

```
BigQuery SQL execution via the `bq` CLI. ACTUAL execution — running, dry-running, counting, pulling rows — not just authoring or charting. ALWAYS USE WHEN: user types "bq", "use bq", "run this", "run this query", "dry-run"; pastes a SELECT against `mozdata`/`moz-fx-data-shared-prod`/`firefox_desktop.*`/`telemetry.*`; asks for terminal-output counts of clients/DAU/MAU; pulls OS or Windows/macOS/Linux/Darwin/kernel version distributions; mentions partition filter, `sample_id`, `baseline_clients_daily`, `baseline_clients_last_seen`, `active_users_aggregates_v3`, `events_stream`, or Glean metric extracts; asks which Mozilla telemetry table is cheapest/right for a count; or wants joins across telemetry namespaces actually run. Beats `mozdata:query-writing` whenever the user expects a real result back — including "should I use table X or Y?" framings. Skip only when the user wants saved/visualized/shared queries (use `redash`), or just needs to find a probe with no execution intent (use `mozdata:probe-discovery`).
```

### iteration 5 — train 6/12, test 4/8

```
Run BigQuery SQL against Mozilla telemetry from the terminal. Use whenever the user wants query results — running a SELECT, doing a dry-run for scan cost, counting clients/DAU/MAU, pulling OS/Windows/macOS/Linux version distributions, extracting Glean metrics, or deciding which telemetry table to actually query for a count. Triggers on `bq`, `mozdata`, `moz-fx-data-shared-prod`, `firefox_desktop.*`, `baseline_clients_daily`, `baseline_clients_last_seen`, `active_users_aggregates_v3`, `events_stream`, partition filters, `sample_id`, or any pasted SQL the user expects executed. Also fires for "should I use table X or Y?" and cross-namespace join questions when execution is implied. Skip for saved/visualized/shared queries (use `redash`) or finding a probe with no execution intent (use `mozdata:probe-discovery`).
```
